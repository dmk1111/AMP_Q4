import { PlateBorderDirective } from './plate-border.directive';

describe('PlateBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new PlateBorderDirective();
    expect(directive).toBeTruthy();
  });
});
